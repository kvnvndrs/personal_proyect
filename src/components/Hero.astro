---
import Icon from './Icon.astro';
---

<div class="min-h-screen flex items-center justify-center px-4 sm:px-6 lg:px-8 pt-16 relative overflow-hidden">
  <!-- Background Elements -->
  <div class="absolute top-0 left-0 w-full h-full overflow-hidden -z-10">
    <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-cyber-primary/5 rounded-full blur-[100px] animate-pulse-slow"></div>
    <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-cyber-secondary/5 rounded-full blur-[100px] animate-pulse-slow" style="animation-delay: 2s;"></div>
  </div>

  <div class="max-w-7xl mx-auto w-full z-10">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div class="space-y-8">
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-cyber-primary/30 bg-cyber-primary/5 text-cyber-primary text-xs font-mono tracking-wider animate-fade-in-up">
          <span class="w-2 h-2 rounded-full bg-cyber-primary animate-pulse"></span>
          SYSTEM_STATUS: ONLINE
        </div>

        <div class="space-y-4">
          <h1 class="text-5xl sm:text-6xl lg:text-7xl font-bold text-white tracking-tight">
            <span class="block text-transparent bg-clip-text bg-gradient-to-r from-white to-slate-400">Ingeniero</span>
            <span class="block text-transparent bg-clip-text bg-gradient-to-r from-cyber-primary to-cyber-secondary typing-effect">
              DevSecOps
            </span>
          </h1>
          
          <p class="text-xl text-slate-400 max-w-lg leading-relaxed border-l-2 border-cyber-secondary/30 pl-4">
            Especialista en <span class="text-cyber-secondary">Ciberseguridad</span> y <span class="text-cyber-primary">Automatización</span>. 
            Protegiendo infraestructuras críticas y asegurando el ciclo de vida del desarrollo.
          </p>
        </div>

        <div class="flex flex-wrap gap-4 pt-4">
          <a
            href="#proyectos"
            class="group relative px-8 py-3 bg-cyber-secondary/10 border border-cyber-secondary/50 text-cyber-secondary font-mono rounded overflow-hidden transition-all hover:bg-cyber-secondary hover:text-black hover:shadow-[0_0_20px_rgba(0,243,255,0.4)]"
          >
            <span class="relative z-10 flex items-center gap-2">
              <Icon name="terminal" class="w-4 h-4" />
              INIT_PROJECTS
            </span>
          </a>
          
          <a
            href="#contacto"
            class="group px-8 py-3 border border-slate-700 text-slate-300 font-mono rounded hover:border-cyber-primary hover:text-cyber-primary transition-all"
          >
            CONTACT_ME
          </a>
        </div>

        <div class="flex gap-6 pt-8 border-t border-slate-800/50">
          <a href="#" class="text-slate-400 hover:text-cyber-secondary transition-colors transform hover:scale-110">
            <Icon name="github" class="w-6 h-6" />
          </a>
          <a href="#" class="text-slate-400 hover:text-cyber-secondary transition-colors transform hover:scale-110">
            <Icon name="linkedin" class="w-6 h-6" />
          </a>
          <a href="#" class="text-slate-400 hover:text-cyber-secondary transition-colors transform hover:scale-110">
            <Icon name="mail" class="w-6 h-6" />
          </a>
        </div>
      </div>

      <div class="relative hidden lg:block">
        <!-- Terminal Window -->
        <div class="relative w-full max-w-lg mx-auto bg-cyber-gray/90 backdrop-blur-xl border border-slate-700 rounded-lg shadow-2xl overflow-hidden transform hover:scale-[1.02] transition-transform duration-500 group">
          <!-- Terminal Header -->
          <div class="flex items-center justify-between px-4 py-2 bg-slate-800/50 border-b border-slate-700">
            <div class="flex gap-2">
              <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
              <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
              <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
            </div>
            <div class="text-xs text-slate-400 font-mono">user@devsecops:~</div>
          </div>
          
          <!-- Terminal Body -->
          <div class="p-6 font-mono text-sm space-y-2 h-[400px] overflow-hidden relative">
            <div class="absolute inset-0 bg-scanline pointer-events-none opacity-10"></div>
            
            <div class="text-slate-300">
              <span class="text-cyber-primary">➜</span> <span class="text-cyber-secondary">~</span> initializing security protocols...
            </div>
            <div class="text-slate-300 delay-100 fade-in">
              <span class="text-cyber-primary">➜</span> <span class="text-cyber-secondary">~</span> loading modules:
            </div>
            <div class="pl-4 text-slate-400 delay-200 fade-in">
              [✓] Cloud_Infrastructure<br/>
              [✓] Threat_Detection<br/>
              [✓] Automated_Compliance<br/>
              [✓] Zero_Trust_Architecture
            </div>
            <div class="text-slate-300 delay-300 fade-in">
              <span class="text-cyber-primary">➜</span> <span class="text-cyber-secondary">~</span> running diagnostics...
            </div>
            <div class="text-green-400 delay-500 fade-in">
              SUCCESS: System Secure. Ready for deployment.
            </div>
            <div class="text-slate-300 mt-4 animate-pulse">
              <span class="text-cyber-primary">➜</span> <span class="text-cyber-secondary">~</span> <span class="cursor-blink">_</span>
            </div>
          </div>
        </div>

        <!-- Decorative Elements -->
        <div class="absolute -top-10 -right-10 w-20 h-20 border-t-2 border-r-2 border-cyber-secondary/30 rounded-tr-3xl"></div>
        <div class="absolute -bottom-10 -left-10 w-20 h-20 border-b-2 border-l-2 border-cyber-primary/30 rounded-bl-3xl"></div>
      </div>
    </div>
  </div>
</div>

<style>
  .typing-effect {
    border-right: 2px solid transparent;
    animation: typing 3.5s steps(40, end), blink-caret .75s step-end infinite;
    white-space: nowrap;
    overflow: hidden;
  }

  .cursor-blink {
    animation: blink 1s step-end infinite;
  }

  .bg-scanline {
    background: linear-gradient(to bottom, transparent 50%, rgba(0, 0, 0, 0.5) 51%);
    background-size: 100% 4px;
  }

  .fade-in {
    opacity: 0;
    animation: fadeIn 0.5s forwards;
  }

  .delay-100 { animation-delay: 0.5s; }
  .delay-200 { animation-delay: 1s; }
  .delay-300 { animation-delay: 1.5s; }
  .delay-500 { animation-delay: 2s; }

  @keyframes typing {
    from { width: 0 }
    to { width: 100% }
  }

  @keyframes blink {
    from, to { opacity: 1 }
    50% { opacity: 0 }
  }

  @keyframes fadeIn {
    to { opacity: 1 }
  }

  @keyframes pulse-slow {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.1); }
  }

  .animate-pulse-slow {
    animation: pulse-slow 4s ease-in-out infinite;
  }
</style>
