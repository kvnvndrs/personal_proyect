---
import Icon from './Icon.astro';
---

<div class="min-h-screen flex items-center justify-center px-4 sm:px-6 lg:px-8 py-20">
  <div class="max-w-7xl mx-auto w-full">
    <div class="text-center mb-16 scroll-reveal">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-cyber-secondary/30 bg-cyber-secondary/5 text-cyber-secondary text-xs font-mono mb-4">
        <span>// SYSTEM_TOOLS</span>
      </div>
      <h2 class="text-4xl sm:text-5xl font-bold text-white">
        Arsenal <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyber-primary to-cyber-secondary">Tecnológico</span>
      </h2>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      <!-- Security -->
      <div class="tech-card group">
        <div class="relative h-full p-6 bg-cyber-gray/50 backdrop-blur-sm border border-cyber-slate rounded-lg hover:border-cyber-primary/50 transition-all">
          <div class="flex items-center gap-3 mb-6 border-b border-cyber-slate pb-4">
            <Icon name="shield" class="w-6 h-6 text-cyber-primary" />
            <h3 class="text-xl text-white font-mono">SECURITY_OPS</h3>
          </div>
          <div class="grid grid-cols-2 gap-3">
            {['OWASP', 'Vault', 'Snyk', 'SonarQube', 'Nmap', 'Burp Suite'].map((tech) => (
              <div class="tech-item flex items-center gap-2 p-2 bg-cyber-dark border border-cyber-slate rounded hover:border-cyber-primary/50 transition-all cursor-crosshair group/item">
                <div class="w-1.5 h-1.5 rounded-full bg-cyber-primary/50 group-hover/item:bg-cyber-primary group-hover/item:shadow-[0_0_5px_#00ff41]"></div>
                <span class="text-sm text-slate-300 font-mono">{tech}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- DevSecOps -->
      <div class="tech-card group">
        <div class="relative h-full p-6 bg-cyber-gray/50 backdrop-blur-sm border border-cyber-slate rounded-lg hover:border-cyber-secondary/50 transition-all">
          <div class="flex items-center gap-3 mb-6 border-b border-cyber-slate pb-4">
            <Icon name="gitBranch" class="w-6 h-6 text-cyber-secondary" />
            <h3 class="text-xl text-white font-mono">DEV_SEC_OPS</h3>
          </div>
          <div class="grid grid-cols-2 gap-3">
            {['Jenkins', 'GitLab CI', 'GitHub Actions', 'Terraform', 'Ansible', 'ArgoCD'].map((tech) => (
              <div class="tech-item flex items-center gap-2 p-2 bg-cyber-dark border border-cyber-slate rounded hover:border-cyber-secondary/50 transition-all cursor-crosshair group/item">
                <div class="w-1.5 h-1.5 rounded-full bg-cyber-secondary/50 group-hover/item:bg-cyber-secondary group-hover/item:shadow-[0_0_5px_#00f3ff]"></div>
                <span class="text-sm text-slate-300 font-mono">{tech}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- Cloud -->
      <div class="tech-card group">
        <div class="relative h-full p-6 bg-cyber-gray/50 backdrop-blur-sm border border-cyber-slate rounded-lg hover:border-cyber-accent/50 transition-all">
          <div class="flex items-center gap-3 mb-6 border-b border-cyber-slate pb-4">
            <Icon name="cloud" class="w-6 h-6 text-cyber-accent" />
            <h3 class="text-xl text-white font-mono">CLOUD_INFRA</h3>
          </div>
          <div class="grid grid-cols-2 gap-3">
            {['AWS', 'Azure', 'Docker', 'Kubernetes', 'Istio', 'Helm'].map((tech) => (
              <div class="tech-item flex items-center gap-2 p-2 bg-cyber-dark border border-cyber-slate rounded hover:border-cyber-accent/50 transition-all cursor-crosshair group/item">
                <div class="w-1.5 h-1.5 rounded-full bg-cyber-accent/50 group-hover/item:bg-cyber-accent group-hover/item:shadow-[0_0_5px_#ff003c]"></div>
                <span class="text-sm text-slate-300 font-mono">{tech}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- Monitoring -->
      <div class="tech-card group">
        <div class="relative h-full p-6 bg-cyber-gray/50 backdrop-blur-sm border border-cyber-slate rounded-lg hover:border-white/50 transition-all">
          <div class="flex items-center gap-3 mb-6 border-b border-cyber-slate pb-4">
            <Icon name="activity" class="w-6 h-6 text-white" />
            <h3 class="text-xl text-white font-mono">MONITORING</h3>
          </div>
          <div class="grid grid-cols-2 gap-3">
            {['ELK Stack', 'Splunk', 'Prometheus', 'Grafana', 'Datadog', 'Wazuh'].map((tech) => (
              <div class="tech-item flex items-center gap-2 p-2 bg-cyber-dark border border-cyber-slate rounded hover:border-white/50 transition-all cursor-crosshair group/item">
                <div class="w-1.5 h-1.5 rounded-full bg-white/50 group-hover/item:bg-white group-hover/item:shadow-[0_0_5px_#ffffff]"></div>
                <span class="text-sm text-slate-300 font-mono">{tech}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- Databases -->
      <div class="tech-card group">
        <div class="relative h-full p-6 bg-cyber-gray/50 backdrop-blur-sm border border-cyber-slate rounded-lg hover:border-cyber-primary/50 transition-all">
          <div class="flex items-center gap-3 mb-6 border-b border-cyber-slate pb-4">
            <Icon name="database" class="w-6 h-6 text-cyber-primary" />
            <h3 class="text-xl text-white font-mono">DATA_STORE</h3>
          </div>
          <div class="grid grid-cols-2 gap-3">
            {['PostgreSQL', 'MongoDB', 'Redis', 'Elasticsearch', 'DynamoDB', 'MySQL'].map((tech) => (
              <div class="tech-item flex items-center gap-2 p-2 bg-cyber-dark border border-cyber-slate rounded hover:border-cyber-primary/50 transition-all cursor-crosshair group/item">
                <div class="w-1.5 h-1.5 rounded-full bg-cyber-primary/50 group-hover/item:bg-cyber-primary group-hover/item:shadow-[0_0_5px_#00ff41]"></div>
                <span class="text-sm text-slate-300 font-mono">{tech}</span>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- Languages -->
      <div class="tech-card group">
        <div class="relative h-full p-6 bg-cyber-gray/50 backdrop-blur-sm border border-cyber-slate rounded-lg hover:border-cyber-secondary/50 transition-all">
          <div class="flex items-center gap-3 mb-6 border-b border-cyber-slate pb-4">
            <Icon name="code" class="w-6 h-6 text-cyber-secondary" />
            <h3 class="text-xl text-white font-mono">LANGUAGES</h3>
          </div>
          <div class="grid grid-cols-2 gap-3">
            {['Python', 'Go', 'Bash', 'JavaScript', 'TypeScript', 'Java'].map((tech) => (
              <div class="tech-item flex items-center gap-2 p-2 bg-cyber-dark border border-cyber-slate rounded hover:border-cyber-secondary/50 transition-all cursor-crosshair group/item">
                <div class="w-1.5 h-1.5 rounded-full bg-cyber-secondary/50 group-hover/item:bg-cyber-secondary group-hover/item:shadow-[0_0_5px_#00f3ff]"></div>
                <span class="text-sm text-slate-300 font-mono">{tech}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>

    <div class="mt-16 scroll-reveal">
      <h3 class="text-2xl text-white text-center mb-8 font-mono">CERTIFICATIONS_ACQUIRED</h3>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        {['CISSP', 'CEH', 'AWS Security', 'Kubernetes CKS'].map((cert) => (
          <div class="cert-card p-4 bg-cyber-dark border border-cyber-slate rounded-lg text-center hover:border-cyber-primary hover:shadow-[0_0_10px_rgba(0,255,65,0.2)] transition-all cursor-pointer group">
            <Icon name="shield" class="w-8 h-8 text-cyber-primary mx-auto mb-2 group-hover:scale-110 transition-transform" />
            <span class="text-sm text-slate-300 font-mono group-hover:text-white">{cert}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

<style>
  .scroll-reveal {
    animation: scrollReveal 0.8s ease-out;
  }

  @keyframes scrollReveal {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .tech-card {
    position: relative;
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .tech-card:nth-child(1) { animation-delay: 0s; }
  .tech-card:nth-child(2) { animation-delay: 0.1s; }
  .tech-card:nth-child(3) { animation-delay: 0.2s; }
  .tech-card:nth-child(4) { animation-delay: 0.3s; }
  .tech-card:nth-child(5) { animation-delay: 0.4s; }
  .tech-card:nth-child(6) { animation-delay: 0.5s; }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .cert-card {
    opacity: 0;
    animation: fadeIn 0.5s ease-out forwards;
  }

  .cert-card:nth-child(1) { animation-delay: 0s; }
  .cert-card:nth-child(2) { animation-delay: 0.1s; }
  .cert-card:nth-child(3) { animation-delay: 0.2s; }
  .cert-card:nth-child(4) { animation-delay: 0.3s; }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</style>

