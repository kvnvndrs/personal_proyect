---
import { ShieldCheck, Code, Database, GitBranch, Award, Briefcase } from 'lucide-react';
---

<div class="min-h-screen flex items-center justify-center px-4 sm:px-6 lg:px-8 py-20">
  <div class="max-w-7xl mx-auto w-full">
    <div class="text-center mb-16 scroll-reveal">
      <span class="text-cyan-400 text-sm tracking-wider uppercase">Acerca de mí</span>
      <h2 class="mt-4 text-4xl sm:text-5xl text-white">
        Construyendo <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">sistemas seguros</span>
      </h2>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 mb-16">
      <!-- About Text -->
      <div class="space-y-6 scroll-reveal">
        <p class="text-lg text-slate-300">
          Soy un ingeniero apasionado por la seguridad de la información y las prácticas DevSecOps, 
          con experiencia en la protección de sistemas críticos y datos sensibles.
        </p>
        <p class="text-lg text-slate-300">
          Mi enfoque se centra en integrar la seguridad desde las primeras etapas del desarrollo, 
          implementando controles automatizados y mejores prácticas que garantizan la protección 
          continua de las aplicaciones y la infraestructura.
        </p>
        <p class="text-lg text-slate-300">
          Especializado en compliance, gestión de vulnerabilidades, y la implementación de 
          arquitecturas de seguridad robustas en entornos cloud y on-premise.
        </p>

        <!-- Stats -->
        <div class="grid grid-cols-2 gap-4 pt-6">
          <div class="p-6 bg-slate-800/30 border border-slate-700/50 rounded-xl hover:border-cyan-400/50 transition-all hover:scale-105">
            <Briefcase className="w-8 h-8 text-cyan-400 mb-3" />
            <div class="text-2xl text-white mb-1">5+ Años</div>
            <div class="text-slate-400">Experiencia</div>
          </div>
          <div class="p-6 bg-slate-800/30 border border-slate-700/50 rounded-xl hover:border-cyan-400/50 transition-all hover:scale-105">
            <Award className="w-8 h-8 text-cyan-400 mb-3" />
            <div class="text-2xl text-white mb-1">20+ Proyectos</div>
            <div class="text-slate-400">Completados</div>
          </div>
        </div>
      </div>

      <!-- Skills Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
        <div class="skill-card p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl backdrop-blur-sm hover:border-cyan-400/50 transition-all group">
          <div class="w-12 h-12 bg-cyan-400/10 rounded-lg flex items-center justify-center mb-4 group-hover:bg-cyan-400/20 transition-colors">
            <ShieldCheck className="w-6 h-6 text-cyan-400" />
          </div>
          <h3 class="text-xl text-white mb-2">Seguridad de Datos</h3>
          <p class="text-slate-400 text-sm leading-relaxed">Implementación de medidas de protección, cifrado y cumplimiento normativo.</p>
        </div>

        <div class="skill-card p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl backdrop-blur-sm hover:border-cyan-400/50 transition-all group">
          <div class="w-12 h-12 bg-cyan-400/10 rounded-lg flex items-center justify-center mb-4 group-hover:bg-cyan-400/20 transition-colors">
            <Code className="w-6 h-6 text-cyan-400" />
          </div>
          <h3 class="text-xl text-white mb-2">DevSecOps</h3>
          <p class="text-slate-400 text-sm leading-relaxed">Integración de seguridad en el ciclo de vida del desarrollo.</p>
        </div>

        <div class="skill-card p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl backdrop-blur-sm hover:border-cyan-400/50 transition-all group">
          <div class="w-12 h-12 bg-cyan-400/10 rounded-lg flex items-center justify-center mb-4 group-hover:bg-cyan-400/20 transition-colors">
            <Database className="w-6 h-6 text-cyan-400" />
          </div>
          <h3 class="text-xl text-white mb-2">Gestión de Datos</h3>
          <p class="text-slate-400 text-sm leading-relaxed">Diseño y protección de arquitecturas de datos seguras y escalables.</p>
        </div>

        <div class="skill-card p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl backdrop-blur-sm hover:border-cyan-400/50 transition-all group">
          <div class="w-12 h-12 bg-cyan-400/10 rounded-lg flex items-center justify-center mb-4 group-hover:bg-cyan-400/20 transition-colors">
            <GitBranch className="w-6 h-6 text-cyan-400" />
          </div>
          <h3 class="text-xl text-white mb-2">CI/CD Seguro</h3>
          <p class="text-slate-400 text-sm leading-relaxed">Pipelines automatizados con controles de seguridad integrados.</p>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .scroll-reveal {
    opacity: 0;
    animation: scrollReveal 0.8s ease-out forwards;
  }

  @keyframes scrollReveal {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .skill-card {
    animation: fadeInUp 0.6s ease-out forwards;
    animation-delay: calc(var(--i, 0) * 0.1s);
  }

  .skill-card:nth-child(1) { --i: 0; }
  .skill-card:nth-child(2) { --i: 1; }
  .skill-card:nth-child(3) { --i: 2; }
  .skill-card:nth-child(4) { --i: 3; }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .skill-card:hover {
    transform: translateY(-5px) scale(1.02);
  }
</style>
