---
import { Mail, MapPin, Phone, Send, Github, Linkedin, Twitter } from 'lucide-react';
---

<div class="min-h-screen flex items-center justify-center px-4 sm:px-6 lg:px-8 py-20">
  <div class="max-w-7xl mx-auto w-full">
    <div class="text-center mb-16 scroll-reveal">
      <span class="text-cyan-400 text-sm tracking-wider uppercase">Contacto</span>
      <h2 class="mt-4 text-4xl sm:text-5xl text-white">
        Trabajemos <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">juntos</span>
      </h2>
      <p class="mt-4 text-xl text-slate-400 max-w-3xl mx-auto">
        ¿Tienes un proyecto en mente? Hablemos sobre cómo puedo ayudarte a asegurar tu infraestructura
      </p>
    </div>

    <div class="grid lg:grid-cols-3 gap-8">
      <!-- Contact Info -->
      <div class="lg:col-span-1 space-y-6">
        <!-- Info Cards -->
        <div class="contact-card p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl hover:border-cyan-400/50 transition-all group">
          <div class="w-12 h-12 bg-cyan-400/10 rounded-lg flex items-center justify-center mb-4 group-hover:bg-cyan-400/20 transition-colors">
            <Mail className="w-6 h-6 text-cyan-400" />
          </div>
          <h3 class="text-lg text-white mb-2">Email</h3>
          <a href="mailto:contacto@devsecops.com" class="text-slate-400 hover:text-cyan-400 transition-colors">
            contacto@devsecops.com
          </a>
        </div>

        <div class="contact-card p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl hover:border-cyan-400/50 transition-all group">
          <div class="w-12 h-12 bg-cyan-400/10 rounded-lg flex items-center justify-center mb-4 group-hover:bg-cyan-400/20 transition-colors">
            <Phone className="w-6 h-6 text-cyan-400" />
          </div>
          <h3 class="text-lg text-white mb-2">Teléfono</h3>
          <a href="tel:+1234567890" class="text-slate-400 hover:text-cyan-400 transition-colors">
            +1 (234) 567-890
          </a>
        </div>

        <div class="contact-card p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl hover:border-cyan-400/50 transition-all group">
          <div class="w-12 h-12 bg-cyan-400/10 rounded-lg flex items-center justify-center mb-4 group-hover:bg-cyan-400/20 transition-colors">
            <MapPin className="w-6 h-6 text-cyan-400" />
          </div>
          <h3 class="text-lg text-white mb-2">Ubicación</h3>
          <p class="text-slate-400">
            Ciudad, País<br />
            Trabajo remoto disponible
          </p>
        </div>

        <!-- Social Links -->
        <div class="p-6 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-xl">
          <h3 class="text-lg text-white mb-4">Redes Sociales</h3>
          <div class="flex gap-3">
            <a
              href="#"
              class="w-10 h-10 rounded-lg bg-slate-800/50 border border-slate-700 flex items-center justify-center text-slate-400 hover:text-cyan-400 hover:border-cyan-400 transition-all hover:scale-110"
            >
              <Github className="w-5 h-5" />
            </a>
            <a
              href="#"
              class="w-10 h-10 rounded-lg bg-slate-800/50 border border-slate-700 flex items-center justify-center text-slate-400 hover:text-cyan-400 hover:border-cyan-400 transition-all hover:scale-110"
            >
              <Linkedin className="w-5 h-5" />
            </a>
            <a
              href="#"
              class="w-10 h-10 rounded-lg bg-slate-800/50 border border-slate-700 flex items-center justify-center text-slate-400 hover:text-cyan-400 hover:border-cyan-400 transition-all hover:scale-110"
            >
              <Twitter className="w-5 h-5" />
            </a>
          </div>
        </div>
      </div>

      <!-- Contact Form -->
      <div class="lg:col-span-2">
        <div class="contact-form-container relative group">
          <div class="absolute -inset-0.5 bg-gradient-to-r from-cyan-500 to-blue-500 rounded-2xl opacity-0 group-hover:opacity-100 blur transition-opacity duration-500"></div>
          <div class="relative p-8 bg-slate-900/90 backdrop-blur-sm border border-slate-700/50 rounded-2xl">
            <form id="contact-form" class="space-y-6">
              <div class="grid md:grid-cols-2 gap-6">
                <div>
                  <label for="name" class="block text-sm text-slate-300 mb-2">Nombre</label>
                  <input
                    type="text"
                    id="name"
                    name="name"
                    required
                    class="w-full px-4 py-3 bg-slate-800/50 border border-slate-700 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:border-cyan-400 transition-colors"
                    placeholder="Tu nombre"
                  />
                </div>
                <div>
                  <label for="email" class="block text-sm text-slate-300 mb-2">Email</label>
                  <input
                    type="email"
                    id="email"
                    name="email"
                    required
                    class="w-full px-4 py-3 bg-slate-800/50 border border-slate-700 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:border-cyan-400 transition-colors"
                    placeholder="tu@email.com"
                  />
                </div>
              </div>

              <div>
                <label for="subject" class="block text-sm text-slate-300 mb-2">Asunto</label>
                <input
                  type="text"
                  id="subject"
                  name="subject"
                  required
                  class="w-full px-4 py-3 bg-slate-800/50 border border-slate-700 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:border-cyan-400 transition-colors"
                  placeholder="¿En qué puedo ayudarte?"
                />
              </div>

              <div>
                <label for="message" class="block text-sm text-slate-300 mb-2">Mensaje</label>
                <textarea
                  id="message"
                  name="message"
                  required
                  rows="6"
                  class="w-full px-4 py-3 bg-slate-800/50 border border-slate-700 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:border-cyan-400 transition-colors resize-none"
                  placeholder="Cuéntame sobre tu proyecto..."
                ></textarea>
              </div>

              <button
                type="submit"
                class="w-full flex items-center justify-center gap-2 px-8 py-4 bg-gradient-to-r from-cyan-500 to-blue-500 rounded-lg text-white hover:shadow-lg hover:shadow-cyan-500/50 transition-all hover:scale-105"
              >
                <Send className="w-5 h-5" />
                Enviar Mensaje
              </button>
            </form>

            <div id="form-message" class="hidden mt-4 p-4 rounded-lg"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Call to Action -->
    <div class="mt-20 text-center">
      <div class="inline-block p-8 bg-gradient-to-br from-slate-800/50 to-slate-900/50 border border-slate-700/50 rounded-2xl scroll-reveal">
        <h3 class="text-2xl text-white mb-4">¿Necesitas consultoría en seguridad?</h3>
        <p class="text-slate-400 mb-6 max-w-2xl">
          Ofrezco servicios de consultoría especializada en seguridad de aplicaciones, 
          auditorías de infraestructura y diseño de arquitecturas DevSecOps.
        </p>
        <a
          href="#contacto"
          class="inline-flex items-center gap-2 px-8 py-3 bg-gradient-to-r from-cyan-500 to-blue-500 rounded-lg text-white hover:shadow-lg hover:shadow-cyan-500/50 transition-all hover:scale-105"
        >
          Agenda una consulta
        </a>
      </div>
    </div>
  </div>
</div>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const formMessage = document.getElementById('form-message');

  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    
    // Simulate form submission
    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());
    
    console.log('Form submitted:', data);
    
    // Show success message
    if (formMessage) {
      formMessage.textContent = '¡Mensaje enviado con éxito! Te responderé pronto.';
      formMessage.className = 'mt-4 p-4 rounded-lg bg-cyan-400/10 border border-cyan-400/50 text-cyan-400';
      formMessage.classList.remove('hidden');
      
      // Reset form
      form.reset();
      
      // Hide message after 5 seconds
      setTimeout(() => {
        formMessage.classList.add('hidden');
      }, 5000);
    }
  });
</script>

<style>
  .scroll-reveal {
    animation: scrollReveal 0.8s ease-out;
  }

  @keyframes scrollReveal {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .contact-card {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .contact-card:nth-child(1) { animation-delay: 0s; }
  .contact-card:nth-child(2) { animation-delay: 0.1s; }
  .contact-card:nth-child(3) { animation-delay: 0.2s; }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .contact-card:hover {
    transform: translateY(-5px) scale(1.02);
  }

  .contact-form-container {
    opacity: 0;
    animation: fadeIn 0.8s ease-out 0.3s forwards;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  input:focus,
  textarea:focus {
    box-shadow: 0 0 0 3px rgba(34, 211, 238, 0.1);
  }
</style>
